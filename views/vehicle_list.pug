extends layout

block content
  h1= title

  ul
    each vehicle in vehicle_list
      li
        a(href=vehicle.url) #{vehicle.model}
        |  (#{vehicle.make})

    else
      li There are no vehicles.