extends layout

block content
  h1= title

  ul
    each people in people_list
      li
        a(href=people.url) #{people.name}
        |  (#{people.lifespan})

    else
      li There are no people.